---
# Example inventory for running-sprawl

# NOTE: Some of these nodes are running on NetBSD because it's way cooler than Linux.
#       https://docs.ansible.com/ansible/latest/os_guide/intro_bsd.html

# NOTE: Some of these nodes are running Tribblix because it's even cooler than NetBSD.
#       I'm gonna figure out how to make Ansible work with Tribblix becuase why not.
#       https://tribblix.org

[database]
# Tribblix
database1 ansible_connection=paramiko ansible_host=example.com ansible_user=user ansible_become_method=su ansible_become_user=root ansible_become_password=password ansible_python_interpreter=/usr/bin/python

[oauth]

[gitserver]

